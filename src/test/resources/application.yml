server:
  port: 8080

build.name: abc-tech-api
build.version: "1.0"

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

abctech:
  app:
    jwtSecret: 3718293U1I
    jwtCookieName: abctechapi
    jwtExpirationMs: 86400000